apiVersion: v1
kind: Service
metadata:
  name: { { include "money-pulse.fullname" . } }
  labels: { { - include "money-pulse.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "money-pulse.selectorLabels" . | nindent 4 } }
