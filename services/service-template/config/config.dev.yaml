app:
  environment: "development"

server:
  port: 8080

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  name: "service_template"
  sslmode: "disable"

log:
  level: "debug"
  format: "console"

jwt:
  secret: "dev-secret-key"

cache:
  enabled: true
  type: "memory"
