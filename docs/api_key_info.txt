API Key Information

The API key for the AI service is: your-api-key

To use this API key with the AI service:

1. In the FastAPI Swagger UI (http://localhost:8001/docs):
   - Click the "Authorize" button at the top right
   - Enter the API key (your-api-key) in the value field
   - Click "Authorize"

2. Now you can make requests to the AI service endpoints with the API key automatically included.

The KYC service is already configured to use this API key when communicating with the AI service.
