apiVersion: v1
kind: ConfigMap
metadata:
  name: users-config
  namespace: money-pulse
data:
  LOG_LEVEL: "info"
  ENVIRONMENT: "production"
  HTTP_PORT: "8080"
  GRPC_PORT: "50052"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
  namespace: money-pulse
  labels:
    app: users-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users-service
  template:
    metadata:
      labels:
        app: users-service
    spec:
      containers:
      - name: users-service
        image: ghcr.io/adil-faiyaz98/money-pulse/users-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 50052
          name: grpc
        env:
        - name: DATABASE_URL
          value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/$(POSTGRES_DB)?sslmode=disable"
        envFrom:
        - configMapRef:
            name: users-config
        - secretRef:
            name: postgres-secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "300m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: money-pulse
  labels:
    app: users-service
spec:
  selector:
    app: users-service
  ports:
  - name: http
    port: 80
    targetPort: http
  - name: grpc
    port: 50052
    targetPort: grpc
  type: ClusterIP